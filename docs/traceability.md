# Plnění požadavků a trasovatelnost

**Účel:** odpověď na otázku „požadavek F5 — kde je splněný a co to dokazuje?". Rejstřík zadání ([`requirements.md`](./requirements.md)) proti kódu a testům, v jednom směru: **požadavek → implementace → důkaz**. Opačný směr — rozhodnutí → požadavek — nese tabulka v [`decisions/README.md`](./decisions/README.md); scénáře, ze kterých požadavky vzešly, [`use-cases.md`](./use-cases.md).

Je to tvrzení o **současném stavu**, tedy týž žánr jako [`architecture.md`](./architecture.md); samostatný soubor je jen proto, že architektura má přes 130 kB a rejstřík se v ní nedá najít. Kanonický je nárok verze v `architecture.md` §9 — když se tahle tabulka s ním rozejde, platí §9 a chyba je tady; nad §9 samotným pak platí anglická sekce *Guarantees* v kořenovém [`README.md`](../README.md), jak říká úvod §9.

**Slovník stavu** je týž tříhodnotový jako v §9:

- **nárokované** — verze na to slibuje spoleh v celém rozsahu, jak požadavek zní;
- **nárokované v užším rozsahu** — slibuje méně, než jak požadavek čte samo zadání, a zúžení je v §9 vyslovené;
- **vyňaté** — verze netvrdí nic. Vyňato není totéž co chybějící: oblast může v repozitáři být a běžet.

Stav neříká „hotovo/nehotovo", ale **co o tom tvrdíme**. Číslo požadavku se v testech samotných vyskytuje jen tam, kde je zrovna po ruce, a nerovnoměrně; závazná vazba je tahle tabulka, ne komentáře v kódu.

---

## Funkční požadavky

| # | Stav | Kde je implementovaný | Co to dokazuje |
|---|---|---|---|
| **F1** — komplexní identifikátory v IR | nárokované | `Model` — `PrimaryKey`, pořadí a typy částí klíče, strategie generování (rozhodnutí [011](./decisions/011-key-generation-strategy-vocabulary.md)); `architecture.md` §4.2 | `Combined/CompositeKeyTest`, `Combined/PrimaryKeyTest`, klíče o dvou až čtyřech částech ve schématu `Database/TestSchemaFixtureTest` |
| **F2** — překlad klíčů ve všech frameworcích | nárokované | parsery a buildery všech tří wrapperů; ploché vykreslení a identitní členy (rozhodnutí [006](./decisions/006-flat-composite-key-rendering.md)), klíčová třída (rozhodnutí [031](./decisions/031-key-class-as-declaration-of-key-parts.md)) | `NHibernate/NHibernateCompositeIdTest`, `NHibernate/NHibernateEmbeddedKeyClassTest`, `EFCore/EFCoreKeyStrategyTest`, `Combined/EnforcedMembersTest`; 3. stupeň ověření `Verification/EmbeddedKeyClassVerificationTest` — výstup se přeloží a framework ho přijme. **Od 2026-09-21 má složený klíč i na javové straně 4. stupeň** (rozhodnutí [087](./decisions/087-an-integration-test-is-a-run-against-the-database.md)): ve všech třech javových cílech a ve všech třech zdrojových směrech se objednávka uloží a najde zpátky podle obou částí klíče — u obou JPA cílů instancí generované třídy `@IdClass`, takže obě části porovnává framework, a klíč lišící se v jedné části nesmí najít nic |
| **F3** — cizí klíče na složené identifikátory | nárokované | `Relation` na `EntityMap` (rozhodnutí [001](./decisions/001-entity-reference-by-name.md)), vykreslení FK (rozhodnutí [012](./decisions/012-foreign-key-rendering.md)), N:M jako spojovací entita (rozhodnutí [005](./decisions/005-many-to-many-as-explicit-junction-entity.md)) | `Combined/RelationModelTest`, `Combined/JunctionEntitySynthesisTest`, `NHibernate/NHibernateKeyManyToOneTest`, `EFCore/EFCoreForeignKeyTest`, `Verification/ManyToManyJunctionVerificationTest`, `Verification/KeyManyToOneVerificationTest`. **Kritérium F3 žádá spuštěný test pro každý typ vztahu** — 4. stupeň ověření běží pro všechny tři a v obou směrech: 1:N (`Verification/DapperToNHibernatePersistenceTest`, `Verification/DapperToEFCorePersistenceTest` — uložení a načtení včetně kolekce), 1:1 sdíleným klíčem (`Verification/DapperOneToOnePersistenceTest`) a M:N průchodem přes syntetizovanou junction entitu (`Verification/DapperManyToManyPersistenceTest`) |
| **F4** — načtení metadat z katalogu | nárokované | `DatabaseCatalog` — `SqlServerCatalogReader`, jediné místo, které čte databázi (rozhodnutí [015](./decisions/015-mapping-fact-completion-from-the-catalog.md)); unikátní omezení čte čtvrtý dotaz a nese je mezireprezentace (rozhodnutí [055](./decisions/055-unique-constraint-as-a-carried-mapping-fact.md)) | `Catalog/SqlServerCatalogReaderTest` proti skutečnému SQL Serveru: typy, délky, přesnost, nullabilita, identita, pořadí čtyřdílného klíče, vícesloupcové cizí klíče, jednosloupcové i vícesloupcové unikátní omezení, spojovací tabulky; `Catalog/CatalogUniqueConstraintTest` pro doplnění omezení do modelu. Očekávanou odpovědí je skript schématu (rozhodnutí [016](./decisions/016-generated-artifact-verification-levels.md)), takže podíl podle F4 je měřitelný — jako číslo se ale nevykazuje |
| **F5** — sloučení metadat z více zdrojů | nárokované | fáze doplňování mezi parserem a builderem; priorita zdrojů (rozhodnutí [017](./decisions/017-source-precedence-for-mapping-facts.md), pro klíč [036](./decisions/036-primary-key-under-source-precedence.md)) | `Combined/SourcePrecedenceTest` — 13 scénářů včetně konfliktu nullability, názvu sloupce, typu a primárního klíče, tedy všech čtyř, které kritérium F5 jmenuje; `Combined/LanguageFactPrecedenceTest` — táž pravidla pro jazyková fakta vlastnosti (rozhodnutí [049](./decisions/049-language-facts-under-source-precedence.md)); `Combined/DiagnosticsTest` |
| **F6** — úplné mapování z neúplného vstupu | nárokované | doplnění z katalogu nad dapperovským vstupem; `architecture.md` §5.2 | `Verification/DapperToEFCoreVerificationTest`, `Verification/DapperToNHibernateVerificationTest` (2.–3. stupeň), `Verification/DapperToNHibernatePersistenceTest` a `Verification/DapperToEFCorePersistenceTest` (4. stupeň — doplněná entita skutečně uloží a načte řádek), `Catalog/CatalogCompletionTest`, `Catalog/CatalogManyToManyDetectionTest`, `Catalog/CatalogInverseCollectionTest` |
| **F7** — Hibernate | nárokované v užším rozsahu | `HibernateWrappers` nad `JakartaPersistence` a `JavaEntityParsing` (rozhodnutí [076](./decisions/076-java-wrappers-in-csharp-jvm-in-containers.md) a [077](./decisions/077-hibernate-wrapper-over-the-shared-jpa-layer.md)): parser anotací a `orm.xml`, builder v explicitním stylu JPA, parser a builder JPQL; `architecture.md` §5 | Kritérium má tři věty a doložené jsou všechny tři. **Dvacet testů entit a patnáct testů dotazů** v xUnit: `Hibernate/HibernateEntityParserTest` a `Hibernate/HibernateOrmXmlTest` (čtení), `Hibernate/HibernateEntityBuilderTest` (tvar), `Hibernate/HibernateToHibernateTest` (round trip entity i dotazu), `Hibernate/HibernateJpqlQueryParserTest` a `Hibernate/HibernateJpqlQueryBuilderTest` (dotazy), Hibernate v maticích `Combined/CrossFrameworkInputs` (šestatřicet směrů) a `Combined/EnforcedMembersTest`. **„Generované projekty se sestaví"** dokládá od 2026-09-18 javová sada `JavaTests/` (rozhodnutí [078](./decisions/078-java-suite-as-a-client-of-a-running-instance.md)): `Hibernate/GeneratedArtifactTest` si artefakty vyžádá od běžící instance přes `/convert`, přeloží je `javac`em, předloží je Hibernate a produkt uloží a načte zpět proti SQL Serveru — ve třech zdrojových směrech (Hibernate, EF Core a NHibernate → Hibernate) a s negativní polovinou, která měří, co Hibernate skutečně odmítá (`architecture.md` §6.2). Vedle toho `HibernateClaimsTest` soudí obě tvrzení rozhodnutí 077, která xUnit ověřit neumí: entity join s `on` ve všech pozicích, kde ho builder vydává, potvrdila; `@Column(precision = n)` na `LocalDateTime` jako `datetime2(n)` vyvrátila (Hibernate 7.4.5 atribut ignoruje, čte `secondPrecision`), což od 2026-09-18 opravilo rozhodnutí [079](./decisions/079-fractional-second-precision-as-second-precision.md). Zúžení je jen to, co §9 u F7 vyslovuje: Hibernate sám za sebe, ne javový ekosystém, a vztahy v rozsahu mezireprezentace |
| **F8** — MyBatis | nárokované v užším rozsahu | `MyBatisWrappers` nad dvěma jazykovými vrstvami a nad žádnou vrstvou frameworku (rozhodnutí [084](./decisions/084-mybatis-wrapper-over-the-shared-sql-reading.md)): deskriptor bez profilu, entitní parser doménové třídy, parser rozhraní mapperu i XML mapperu na entitním průchodu, dotazové parsery obou forem a builder nad sdíleným `TransactSql`; `architecture.md` §5 | Kritérium má tři počty a v xUnit jsou všechny tři. **Testy mapování:** `MyBatis/MyBatisMappingParserTest` (dvojice sloupec–vlastnost, `jdbcType` jako rodina s facetami, `<id>` jako identita výsledku a ne klíč, dva `<resultMap>` téže třídy jako `Conflict` se zachovanou první hodnotou, `extends`, vnořené mapování zakládající druhou entitu a vztah bez sloupců, uzavřené mapování proti otevřenému, `@Results` a `@Many`) a `MyBatis/MyBatisBuilderTest` (dokument s prologem, DOCTYPE a uzavřeným `<resultMap>`, složený klíč v pořadí částí, nepersistovaná vlastnost vynecháním, mechanické ztráty, doslovný typ zdroje, vztah bez místa, POJO bez jediného importu z frameworku a bez konstruktoru), k tomu MyBatis v maticích `Combined/CrossFrameworkInputs` (šestatřicet směrů) a `Combined/EnforcedMembersTest` a řádek v `Combined/TargetFrameworkDescriptorTest`. **Testy dotazů:** `MyBatis/MyBatisQueryTest` (statické značky `<include>`, `<where>` a `<trim>`, značka vedená OGNL, `<foreach>` v kanonickém tvaru i mimo něj, `#{}` jako parametr, `${}` a už přítomné `@` jako odmítnutí, skalár z podpisu metody i z `javaType` a jeho `Conflict` proti odvozenému, souběh obou forem téhož `<namespace, id>`, `@SelectProvider`, ruční join, poddotaz, víc příkazů v jednom dokumentu) a `Combined/QueryMatrixTest` nad jedenácti směry, kterých se MyBatis účastní; `Combined/SharedSqlReadingTest` k tomu dokládá, že týž text přečtený z dapperovské jednotky, ze `<sql-query>` a z mapperu MyBatisu dá tutéž mezireprezentaci (rozhodnutí 082). **Javová sada dostala 2026-09-20 třetí cíl a téhož dne proběhla zelená v zafixovaném prostředí**: `mybatis/GeneratedArtifactTest` (2. až 4. stupeň ve třech zdrojových směrech — `javac` nad doménovými třídami i nad rozhraním, stavba `SqlSessionFactory` nad dokumenty mapperů, spuštění generovaného příkazu proti databázi — a negativní polovina, ve které MyBatis sám odmítne projekt s týmž příkazem vysloveným v anotaci i v XML), `MyBatisClaimsTest` (tři tvrzení rozhodnutí 084: vynucený bezparametrický konstruktor, uzavřenost mapování při `autoMapping="false"` proti otevřenému, navázání kolekce vydaným `<foreach>`) a `MyBatisBootstrap` s programovou `Configuration`, protože `mybatis-config.xml` je fakt konzumentského projektu. Běh: profil `test` v compose, obraz Mavenu s Temurinem 25, SQL Server 2022, 65 testů bez selhání a bez přeskočení. **F8 tím vystoupilo z vyňaté oblasti 6** (§9 [`architecture.md`](./architecture.md), sekce *Guarantees* kořenového [`README.md`](../README.md)). Zúžení je to, co §9 u F8 vyslovuje: MyBatis sám za sebe, ne javový ekosystém, vztahy v rozsahu mezireprezentace a **dynamický příkaz nástroj nepřeloží** — `<if>`, `<choose>` a `<bind>` dělají z jednoho `<select>` rodinu příkazů, kterou mezireprezentace nenese, takže dotaz dostane `Failure`, který značku jmenuje |
| **F9** — EclipseLink | nárokované v užším rozsahu | `EclipseLinkWrappers` nad `JakartaPersistence` a `JavaEntityParsing` (rozhodnutí [080](./decisions/080-eclipselink-as-the-second-profile-over-the-jpa-layer.md)): deskriptor s profilem implementace, builder s nationalizací doslovným typem, parser s hlášením líné reference a dva prázdné háčky; `architecture.md` §5 | Kritérium má tři počty a v xUnit jsou všechny tři. **Testy mapování:** `EclipseLink/EclipseLinkEntityBuilderTest` (profil, `AUTO` jako vypsaná tabulka čítače, nationalizace doslovným typem i její ztráta, klíčová třída, explicitní názvy) a `EclipseLink/EclipseLinkEntityParserTest` (standardní anotace, nematerializované defaulty, vendor anotace, líná reference, `orm.xml`), k tomu EclipseLink v maticích `Combined/CrossFrameworkInputs` (šestatřicet směrů) a `Combined/EnforcedMembersTest` a řádek v `Combined/TargetFrameworkDescriptorTest`. **Testy dotazů:** `EclipseLink/EclipseLinkQueryTest` (typovaný i netypovaný dotaz, stránkování na dotazovém objektu, round trip, odmítnutý `limit`) a `Combined/QueryMatrixTest`, který bere směry z výčtu: dvě tvrzení (artefakt vznikne, nic nezmizí mlčky) nad jedenácti směry, kterých se EclipseLink od vstupu MyBatisu účastní, a k tomu řádky o jazyce cíle a seskupené dotazy `Combined/GroupedQueryTest` — dohromady přes dvacet případů. **Překlady mezi Hibernate a EclipseLinkem:** `EclipseLink/HibernateToEclipseLinkTest`, sedm v obou směrech (`AUTO` jako tabulka i jako sekvence, nationalizace tam i zpět, vendor anotace obou implementací, líná reference, složený klíč, stránkování HQL na dotazový objekt). **Javová polovina sady poprvé proběhla 2026-09-18** (`eclipselink/GeneratedArtifactTest` — 2. až 4. stupeň ve třech zdrojových směrech, `EclipseLinkClaimsTest` — obě tvrzení profilu potvrzená): tři předpoklady rozhodnutí opravila a pak prošla. První běh byl na hostiteli, **v zafixovaném prostředí sada proběhla téhož dne** — profil `test` v compose, obraz Mavenu s Temurinem 25, SQL Server 2022, 44 testů bez selhání a bez přeskočení (`architecture.md` §6.2) —, čímž požadavek z vyňaté oblasti 6 **vystoupil**. Zúžení je jen to, co §9 u F9 vyslovuje: EclipseLink sám za sebe, ne javový ekosystém, vztahy v rozsahu mezireprezentace a **líné načtení reference nástroj netvrdí** (bez weavingu je `fetch = LAZY` na referenci tiše eager) |
| **F10** — cross-ecosystem překlad | vyňaté | pivot přes mezireprezentaci: .NET wrappery a oba javové (rozhodnutí [077](./decisions/077-hibernate-wrapper-over-the-shared-jpa-layer.md) a [080](./decisions/080-eclipselink-as-the-second-profile-over-the-jpa-layer.md)) se potkávají jen v `AbstractWrappers` | `Combined/CrossEcosystemTest` — první scénáře obou směrů (hbm.xml → anotace včetně generátoru a klíčové třídy, javová entita → EF Core a NHibernate, JPQL → SQL, LINQ a HQL) a osmnáct směrů napříč ekosystémy v maticích `Combined/CrossFrameworkInputs` (od 2026-09-18, kdy je EclipseLink rozšířil ze šesti na dvanáct, a od 2026-09-20, kdy je MyBatis rozšířil na osmnáct); od 2026-09-18 navíc javová sada ověřuje směry EF Core → Hibernate a NHibernate → Hibernate až po běh proti databázi (`Hibernate/GeneratedArtifactTest` v `JavaTests/`, viz F7). Od 2026-09-20 stojí na každé straně matice framework, který mapování vyslovuje, i framework, který ho nevyslovuje, v obou ekosystémech — dvojice Dapper ↔ MyBatis je první doložený pár, kde se F6 dá měřit oběma směry. **Od 2026-09-21 si matice počítá sama** (rozhodnutí [090](./decisions/090-the-cross-ecosystem-matrix-counts-itself.md), `Combined/CrossEcosystemMatrixTest`): ekosystém vyslovuje deskriptor, cross-ecosystem překladem je celý převod jednoho scénáře přes hranici a end-to-end scénářem takový překlad dovedený k běhu proti databázi. Obě věty kritéria jsou tím doložené — třicet tři překladů (patnáct vzorkových scénářů a osmnáct dvojic diferenční matice) a scénář ve všech čtyřech uspořádaných dvojicích ekosystémů —, číslo je tady schválně jen jako dnešní stav a mez hlídá test. Zbývá týž krok jako u F12 a F13, **zelený běh obou sad v zafixovaném prostředí**, a do té doby požadavek zůstává **vyňatý** |
| **F11** — validace cílových artefaktů | nárokované v užším rozsahu | kontrola úplnosti před generováním a strukturovaná diagnostika (rozhodnutí [004](./decisions/004-unexpressible-facts-as-warnings.md), [010](./decisions/010-diagnostics-as-returned-data.md)); stupně ověření (rozhodnutí [016](./decisions/016-generated-artifact-verification-levels.md), [027](./decisions/027-query-artifact-verification.md)) | `Combined/DiagnosticsTest` včetně `EmptyConversionTest` — převod, ze kterého nic nevyšlo, to hlásí záznamem místo ticha (rozhodnutí [045](./decisions/045-a-conversion-that-produced-nothing-says-so.md)) —, `Combined/InventedFactsTest`, `Combined/EnforcedMembersTest`, `NHibernate/NHibernateUnreadElementTest`, `NHibernate/NHibernateXmlEscapingTest` — generované mapování je správně utvořené XML i pro název se znakem markupu (rozhodnutí [046](./decisions/046-xml-mapping-written-through-an-element-writer.md)) —, `Combined/QueryFaithfulnessTest` — dotaz, který by vrátil jinou množinu řádků, se nevydá, ani když ho parser nepřečte celý (rozhodnutí [070](./decisions/070-a-parser-refuses-what-would-change-the-row-set.md)), a jedno selhání nesebere entity s sebou (rozhodnutí [053](./decisions/053-a-query-that-would-return-other-rows-is-not-emitted.md)), `Combined/NullableKeyPartTest` a `EFCore/EFCoreLiteralSqlTypeTest` — dva fakty, které dřív mizely bez záznamu (rozhodnutí [052](./decisions/052-literal-sql-type-reaches-the-ef-core-annotation.md) a [054](./decisions/054-nullable-key-part-is-a-reported-loss.md)); celá složka `Verification/`; chybová odpověď rozhraní má tvar, který dokument slibuje (`Api/ConvertEndpointTest`, rozhodnutí [044](./decisions/044-error-response-as-problem-details.md)); `Combined/DualUnitTest` — jednotku, kterou nevzal nikdo, hlásí jeden záznam a ne dva, a záznam neúspěšného dotazu jmenuje polem `query`, o který dotaz jednoho dokumentu šlo (rozhodnutí [081](./decisions/081-a-unit-may-be-a-mapping-and-a-query-at-once.md)); `Combined/SharedSqlReadingTest` — nativní dotaz ze `<sql-query>` se zástupným symbolem NHibernate je `Failure`, který symbol jmenuje, a jeho návratové mapování `Loss`, po kterém dotaz přesto vyjde (rozhodnutí [082](./decisions/082-t-sql-read-and-written-by-a-shared-project.md)). **Od 2026-09-21 sahá `Combined/QueryFaithfulnessTest` i za samotný `SELECT` a za vyjmenované kroky LINQ:** příkaz stojící vedle překládaného `SELECT`u (včetně zapisujícího), klauzule `WITH`, `INTO`, `FOR XML`/`FOR JSON` a `TABLESAMPLE` odmítají artefakt, nápověda tabulky a `OPTION` jsou proti nim `Loss` a artefakt vydají, a kroky `OfType`, `SkipWhile`, `TakeWhile`, `DefaultIfEmpty`, `First`, `Single`, `ElementAt`, `Count`, `Any` a `GroupJoin` odmítají tam, kde do té doby mlčky ubraly filtr nebo výřez; `Include` zůstává ztrátou, což je hranice téhož pravidla z druhé strany. **Od 2026-09-21 ubývá tichá záměna u typu sloupce** (rozhodnutí [086](./decisions/086-target-database-dialect-declared-by-the-descriptor.md), `Verification/CharacterTypeVerificationTest`): tvrzení, pro které NHibernate nemá registrované jméno, nese doslovný typ deklarovaného dialektu na `<column>` a hlásí se `Convention`, doslovný typ zdroje má přednost a nehlásí se nic, a kde dialekt tvrzení vyhláskovat neumí, zůstává dosavadní `Loss` — tři různé odpovědi, žádná z nich mlčení. **Zúžení:** syntaktická správnost se ověřuje testy, ne za běhu — překladová cesta cizí kód nekompiluje (§9) |
| **F12** — testovací sada pro Javu | vyňaté | `ORMConvertor/JavaTests/` — Maven projekt s JUnit mimo řešení .NET, se všemi čtyřmi zafixovanými javovými závislostmi; stupeň `java-tests` v `ORMConvertorAPI/Dockerfile`, služby `java_tests` a `test_app` profilu `test`, job `java-test` workflow (rozhodnutí [076](./decisions/076-java-wrappers-in-csharp-jvm-in-containers.md) a [078](./decisions/078-java-suite-as-a-client-of-a-running-instance.md)); `architecture.md` §6.2 | `TestSchemaTest` — připojení, stavba sdíleného schématu z `Tests/Database/TestSchema.sql` a jeho čtení zpět —, `HibernateClaimsTest` — dvě tvrzení rozhodnutí 077 nad ručně psanými entitami, spuštěná proti SQL Serveru (jedno potvrzené, jedno vyvrácené, viz F7) —, `Hibernate/GeneratedArtifactTest` — 2. až 4. stupeň nad artefakty běžící instance, ve třech zdrojových směrech (viz F7) — a na .NET straně `Combined/TargetFrameworkDescriptorTest.JavaSuiteDependenciesMatchTheJavaDescriptors`, který verzi deskriptoru váže na závislost `pom.xml`; druhou polovinu téže vazby tvrdí javová sada za běhu proti `org.hibernate.Version`. Sada tedy od 2026-09-18 generovaný kód sestaví a spustí; od téhož dne má i javovou polovinu pro EclipseLink (`eclipselink/GeneratedArtifactTest`, `EclipseLinkClaimsTest`, `EclipseLinkBootstrap` s programovou `PersistenceUnitInfo`), která téhož dne poprvé proběhla, tři předpoklady rozhodnutí 080 opravila a pak prošla i v zafixovaném prostředí (profil `test`, SQL Server 2022, 44 testů bez přeskočení — viz F9). **Třetí cíl sada dostala 2026-09-20** s MyBatisem (`mybatis/GeneratedArtifactTest`, `MyBatisClaimsTest`, `MyBatisBootstrap` — programová `Configuration` místo `mybatis-config.xml`, protože konfigurace je fakt konzumentského projektu a nese přihlašovací údaje), takže kritérium „Hibernate, MyBatis i EclipseLink nad stejnou databází a daty“ má čím být doložené. Sada od téhož dne běží zelená v zafixovaném prostředí nad všemi třemi cíli (profil `test`, SQL Server 2022, 67 testů bez selhání a bez přeskočení, §6.2). **Obě velikostní meze kritéria jsou od 2026-09-21 splněné a kontrolované** (rozhodnutí [087](./decisions/087-an-integration-test-is-a-run-against-the-database.md)): integračním testem je 4. stupeň ověření rozhodnutí [016](./decisions/016-generated-artifact-verification-levels.md), nese `@Tag("integration")` a `SuiteSizeTest` obě meze ověřuje proti vlastním přeloženým třídám, takže tenhle řádek žádné číslo neopisuje — `mvn test -Dgroups=integration` tutéž množinu i spustí. Téhož dne přibyl ve všech třech cílech a ve všech třech zdrojových směrech běh složeného klíče proti databázi (`anOrderIsFoundByBothPartsOfItsGeneratedKeyClass`, u MyBatisu `anOrderIsReadBackThroughBothIdColumnsOfItsGeneratedResultMap`). Požadavek zůstává **vyňatý** jen do zeleného běhu v zafixovaném prostředí, protože nárok na javový požadavek se u nás vyslovuje až po něm; na vývojovém hostiteli je sada zelená v počtu 78 testů, z toho 34 integračních |
| **F13** — diferenční ověření výsledků | vyňaté | `Tests/Differential/` a `JavaTests/.../differential/` nad sdílenými soubory v `Tests/Database/Differential/` (rozhodnutí [089](./decisions/089-differential-verification-as-the-fourth-level-over-a-query.md)): šest dotazů, třicet dvojic, každý framework zdrojem, kanonický výsledek na dotaz, pět mutací vydaného artefaktu | Obě kritéria splněná a hlídaná sadami (`DifferentialMatrixTest`), nárok ale až po zeleném běhu v zafixovaném prostředí (§9, oblast 6). Ověření tvaru dotazu (rozhodnutí 027) zůstává .NET předstupněm, ne splněním |
| **F14** — celé třídy a dávkové vstupy v UI | nárokované v užším rozsahu | vícejednotkový převod, výstup po souborech, `/archive`, obrazovky podle rozhodnutí [033](./decisions/033-shape-of-the-static-frontend-screens.md); záznam ukazuje na svou jednotku (rozhodnutí [066](./decisions/066-records-attributed-to-the-input-unit.md)) | `Combined/TranslationPerformanceTest` — jeden převod nad 200 jednotkami; `Combined/ApiContentContractTest`; `Api/ArchiveEndpointTest` — položky se z archivu rozbalí pod týmiž názvy a s týmž obsahem. `Api/ConvertEndpointTest` — jméno poslané s jednotkou se vrací v poli `unit` záznamu; `Combined/DualUnitTest` — dokument, který je mapováním i dotazem zároveň, se posílá vcelku a přečtou ho oba průchody (rozhodnutí [081](./decisions/081-a-unit-may-be-a-mapping-and-a-query-at-once.md)). **Zúžení:** na jednotku ukazují záznamy vzešlé z jejího čtení (jménem od klienta, nebo pořadím `unit N`); záznamy fáze doplnění a generování nesou dál entitu a vlastnost, výstupní artefakty se jednotkám nepřipisují a zobrazení mezireprezentace verze nenárokuje (§9) |
| **F15** — výběr cíle a optimalizace | vyňaté | `Advisor` (ILP přes GLPK) a `AdvisorBenchmarking` běží a jsou v rozhraní | Netestované vcelku (§9, oblast 1) — proto vyňaté |

## Systémové požadavky

| # | Stav | Kde je implementovaný | Co to dokazuje |
|---|---|---|---|
| **S1** — modulární rozšiřitelnost | nárokované | wrapper na framework, společné rozhraní v `AbstractWrappers`, deskriptor cíle (rozhodnutí [009](./decisions/009-target-framework-descriptor.md)); sdílené čtení v `LinqParsing` a `CSharpEntityParsing` (rozhodnutí [026](./decisions/026-home-of-shared-query-reading.md)), na javové straně v `JavaEntityParsing` a `JakartaPersistence` (rozhodnutí [076](./decisions/076-java-wrappers-in-csharp-jvm-in-containers.md) a [077](./decisions/077-hibernate-wrapper-over-the-shared-jpa-layer.md)) a od 2026-09-19 čtení i zápis T-SQL v `TransactSql` — pátá vrstva ekosystému a první, která hranici ekosystémů překračuje (rozhodnutí [082](./decisions/082-t-sql-read-and-written-by-a-shared-project.md)); vytknout gramatiku z Dapper wrapperu byl jednorázový zásah a přidání dalšího čtenáře SQL je od té chvíle čistě přírůstkové; čtvrtý framework vstoupil jako nový projekt wrapperu a `AbstractWrappers` se změnily jen o zápisovou cestu vnořeného klíče a o jazyk artefaktu dotazové metody | `Combined/TargetFrameworkDescriptorTest` — framework přidaný bez deskriptoru neprojde; `Combined/EnforcedMembersTest` váže deklaraci a emisi (rozhodnutí [037](./decisions/037-enforced-member-binding-held-by-the-test.md)). Strukturálně: wrappery nereferencují `DatabaseCatalog` ani jiný wrapper; `Combined/SharedSqlReadingTest` dokládá, že sdílení jazyka je tvrzení o chování — týž text přečtený z Dapper jednotky a z `<sql-query>` dá tutéž mezireprezentaci. **Od 2026-09-21 je vázaný na výčet i vstup do rozhraní:** `Combined/ApiContentContractTest` žádá řádek v `/required-content` pro každou hodnotu `ORMEnum` a v něm mapovací i dotazovou jednotku, a `Combined/EnforcedMembersTest` žádá, aby ručně psaná matice rozhodnutí [037](./decisions/037-enforced-member-binding-held-by-the-test.md) jmenovala každý framework výčtu — bez obojího mohl wrapper vzniknout a přesto zůstat z rozhraní nedosažitelný, nebo projít bez jediného tvrzení o vynucených členech |
| **S2** — determinismus a opakovatelnost | nárokované | buildery bez závislosti na prostředí; zafixované verze (rozhodnutí [013](./decisions/013-target-framework-versions.md), [034](./decisions/034-central-version-management.md)); verze nástroje v záznamu (rozhodnutí [069](./decisions/069-major-marks-a-milestone-not-a-break.md)) | `Combined/RunRecordTest` — dva běhy nad týmž vstupem dávají bajtově shodné artefakty i shodné záznamy, **od 2026-09-21 ve všech šestatřiceti směrech matice**, protože S2 je tvrzení o nástroji a ne o jedné dvojici, a k tomu nad prohozeným pořadím vstupních jednotek; `NHibernate/NHibernateEmbeddedKeyClassTest` (pořadí vstupu nerozhoduje); `Combined/ModelFactoryTest` — invarianty, o které se determinismus opírá, drží tovární metody modelu, ne volající; `Api/ConvertEndpointTest` — artefakt vrácený po drátě je bajtově týž jako z přímého volání orchestrace, takže determinismus nekončí na serializaci (rozhodnutí [043](./decisions/043-rest-contract-guarded-over-http.md)) |
| **S3** — výkon překladu | nárokované | jednofázová pipeline bez kompilace na překladové cestě | `Combined/TranslationPerformanceTest` — 100 entit a 100 dotazů EF Core → NHibernate proti třicetivteřinovému limitu; čtení katalogu se měří odděleně a vydává se v `CatalogReadTime`. Změřeno na dvou strojích lišících se výkonem zhruba trojnásobně, obě čísla nese [`ORMConvertor/README.md`](../ORMConvertor/README.md#translation-performance-s3); limit drží na obou s odstupem nejméně padesátinásobku |
| **S4** — izolace a bezpečnost | první věta **vyňatá**, druhá **nárokovaná** | druhá věta konstrukcí: žádný builder nevypisuje připojení (rozhodnutí [029](./decisions/029-database-connection-is-the-consumer-projects-fact.md), [040](./decisions/040-boundary-of-the-handed-over-artifact.md)) | `Combined/ArtifactCarriesNoCredentialsTest` ve všech devíti směrech, `Combined/ConsumerProjectFactsTest`. **První věta** — izolace spouštění s limity CPU, paměti a času — se nenárokuje; co z ní plyne, popisuje [`threat-model.md`](./threat-model.md) |
| **S5** — přenositelné prostředí | nárokované v užším rozsahu | jeden compose soubor se dvěma profily (rozhodnutí [039](./decisions/039-container-configuration-of-the-environment.md)), `ORMConvertorAPI/Dockerfile` se stupni `tests` a `java-tests`, `database.Dockerfile`; javová sada podle rozhodnutí [076](./decisions/076-java-wrappers-in-csharp-jvm-in-containers.md) jako služba `java_tests` a job `java-test`, a od 2026-09-18 instance nástroje, proti které sada překládá — služba `test_app` z obrazu `runtime` v profilu `test` a publikovaná aplikace na runneru (rozhodnutí [078](./decisions/078-java-suite-as-a-client-of-a-running-instance.md)) | `docker compose --profile test build tests` a po něm `run --rm tests` reprodukují celou .NET sadu na stroji, kde není nic než Docker, a týž pár s `java_tests` javovou sadu — od 2026-09-18 i s instancí nástroje, kterou si profil postaví sám (její první kontejnerový běh 2026-09-17, [*Tests*](../ORMConvertor/README.md#tests)); CI běží obě proti SQL Serveru — .NET s `ORMCONVERTOR_REQUIRE_TEST_DATABASE=1`, takže tiché přeskočení neprojde, javová bez přeskočení z principu. Kontejnerový běh proběhl nad **dnešní** sadou, celý a bez jediného přeskočení, a to na dvou strojích (2026-08-23 a 2026-08-24), znovu 2026-08-25, 2026-09-17 a 2026-09-18 nad oběma sadami (tamtéž, [*Tests*](../ORMConvertor/README.md#tests)) — vždy dvojicí `build` a `run`, protože `run` sám obraz nepřestavuje. Že na tom důkaz stojí, ukázal 2026-08-25 nepřestavěný obraz: ohlásil zeleně a bez přeskočení 507 testů proti 602 z hostitele téhož checkoutu, tedy sadu předchozího dne. Kolik testů to je, vyslovuje [`ORMConvertor/README.md`](../ORMConvertor/README.md#how-large-the-suite-is-and-what-it-covers) jediným číslem. **Zúžení:** experimentální pipeline neexistuje, takže není co kontejnerizovat; javová sada je od 2026-09-17 uvnitř nároku (§9) |
| **S6** — pozorovatelnost | nárokované | `ConversionResult` — identifikátor běhu, verze nástroje a obou frameworků, záznamy, stav katalogu; `architecture.md` §5.1 | `Combined/RunRecordTest`; `Api/ConvertEndpointTest` — záznam dorazí přes HTTP kompletní, s verzí ze sestavení a čerstvým identifikátorem u každého volání; `Api/OpenApiDocumentTest` — dokument OpenAPI nese totéž číslo verze. Verze frameworků pocházejí z deskriptorů, takže záznam nemůže tvrdit nic jiného než generátor (rozhodnutí [013](./decisions/013-target-framework-versions.md)). **Od 2026-09-21 nese záznam z téhož místa i cílový databázový dialekt** (`TargetDatabaseDialect`, rozhodnutí [086](./decisions/086-target-database-dialect-declared-by-the-descriptor.md)), takže proti které databázi platí doslovné typy sloupců v artefaktu, konzument nehádá; shodu všech šesti deskriptorů na jediné dnešní hodnotě hlídá `Combined/TargetFrameworkDescriptorTest.EveryDescriptorDeclaresTheOnlyDialectThisVersionTargets` |
| **S7** — uživatelská přívětivost | nárokované v užším rozsahu | statické stránky bez buildu (rozhodnutí [032](./decisions/032-frontend-as-static-pages-without-a-build.md)), podoba obrazovek [033](./decisions/033-shape-of-the-static-frontend-screens.md); pětikrokový scénář popisuje [`use-cases.md`](./use-cases.md), UC4 | `Combined/ApiContentContractTest` — každá jednotka, kterou rozhraní vyžaduje, má ukázku a jazyk, který zdroj umí přečíst; `Api/RestContractTest` a `Api/ArchiveEndpointTest` — táž vazba přes HTTP a stažení celého výstupu jako ZIP (rozhodnutí [043](./decisions/043-rest-contract-guarded-over-http.md)). **Zúžení:** validace na klientovi je pomocník, ne brána; chyba na úrovni řádku platí pro vstupní syntaxi (§9). Automatizovaný test **frontendu** neexistuje (rozhodnutí [032](./decisions/032-frontend-as-static-pages-without-a-build.md)); REST rozhraní pod ním hlídané je. **Obrazovka si rozpracovaný vstup pamatuje** mezi načteními (rozhodnutí [056](./decisions/056-work-in-progress-input-stays-in-the-browser.md), `architecture.md` §6.3) a pás záznamů nese ke každému druhu vysvětlující větu, filtr podle druhu a odkaz na artefakt, kterého se záznam týká |

## Experimentální požadavky

Všechny jsou **vyňaté** — verze netvrdí T1–T7 vůbec (§9, oblast 6). Sloupec „co je hotové" proto neříká „splněno", ale „co z toho v repozitáři existuje".

| # | Co z toho existuje |
|---|---|
| **T1** — reálná případová studie | nic; scénář popisuje [`use-cases.md`](./use-cases.md), UC5 |
| **T2** — matice překladů | devět .NET směrů v rozsahu projekce, filtrace, join, agregace, řazení, stránkování (rozhodnutí [060](./decisions/060-pagination-as-a-query-instruction.md)), poddotazů v podmínce (rozhodnutí [061](./decisions/061-subquery-as-a-condition-operand.md)) a množinových operací (ty NHibernate podle deskriptoru odmítá záznamem): `Combined/QueryMatrixTest`, `Combined/QueryTargetShapeTest`, `Combined/SetOperationQueryTest`, `Combined/PaginationQueryTest`, `Combined/SubQueryConditionTest`, `Combined/InValueListTest`, `Verification/QueryVerificationTest`. **Od 2026-09-20 se měří na vzorku, který parametr nese** (rozhodnutí [083](./decisions/083-parameter-as-the-fifth-operand-shape.md)): kategorii matice parametr nepřidává, ale mění vzorek všech buněk — filtrace, joiny i poddotazy se nově dají vyhodnotit na dotazech, jaké se v cílových projektech opravdu píšou, a vzorové dotazy NHibernate ho nesou; důkaz `Combined/QueryParameterTest`. **Kategorie *stránkování* se od téhož dne měří na parametrizovaném tvaru** (rozhodnutí [085](./decisions/085-a-row-count-is-a-number-or-a-parameter.md)): literálové stránkování je dotaz na jednu konkrétní stránku a ve zdrojových projektech se nepíše, takže tahle buňka se dosud měřila na tvaru, který se nevyskytuje; důkazy `Combined/PaginationParameterTest`, `Verification/QueryVerificationTest` a v javové sadě `hibernate/GeneratedArtifactTest` a `mybatis/GeneratedArtifactTest`. **Kategorie *agregace* a *seskupení* se od 2026-09-21 měří ve všech směrech** (`Combined/GroupedQueryTest`): dosud je matice nesla jen na dotazu projekce–filtrace–řazení, a právě proto v nich přežily dvě vady, které žádný stupeň ověření nenašel — `COUNT(*)` vypsané T-SQL builderem jako `COUNT(c.*)` a `g.Key` nečtené zpět sdíleným LINQ parserem (§5). Chybí javová strana |
| **T3** — metriky korektnosti | čtyřstupňový model ověření (rozhodnutí [016](./decisions/016-generated-artifact-verification-levels.md)) dává, co se dá počítat; jako čísla se dnes nevykazují |
| **T4** — LLM baseline | nic |
| **T5** — RAG / agentní varianta | nic |
| **T6** — ablace | nic |
| **T7** — baseline optimalizace | ILP model v `Advisor` (GLPK přes P/Invoke) běží; heuristiky ani srovnání nejsou, testy žádné |

---

## Kritéria ověření, která zatím nikdo nespočítal

Kritéria u F1–F15 jsou napsaná jako měřitelná a u většiny z nich měření existuje. Dvě jsou výjimka a je poctivější je jmenovat než je nechat čtenáře hledat:

- **F4** žádá „správně získáno ≥ 95 % sledovaných metadat". Očekávaná odpověď existuje (skript schématu), takže podíl spočítat lze; jako číslo se nikde nevykazuje.
- **T3** je celý o podílech, a ty se budou počítat až v textu práce.

Vedle toho platí, že **kolik kódu testy pokrývají, se od 2026-08-22 měří** — `dotnet test --collect:"XPlat Code Coverage"`, v CI při každém běhu. Naměřená čísla nese [`ORMConvertor/README.md`](../ORMConvertor/README.md#how-large-the-suite-is-and-what-it-covers); tady schválně nejsou, aby se dvě místa nerozcházela.

## Jak se to udržuje

Tabulka je součástí povinného uzavíracího kroku: změna chování aktualizuje `architecture.md` a **řádek zde**, pokud se dotkne toho, co požadavek tvrdí nebo čím se to dokazuje. Nový test do tabulky patří jen tehdy, když je *důkazem* požadavku — ne každý test jím je, a seznam všech testů je adresář `Tests/`.
